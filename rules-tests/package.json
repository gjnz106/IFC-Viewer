{
  "name": "ifc-delta-rules-tests",
  "private": true,
  "type": "module",
  "description": "Emulator tests for firestore.rules (per-project roles)",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project ifc-delta-test \"node firestore.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.0.0"
  }
}
